# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
dist-ssr/
# Storybook's static build (`pnpm build-storybook`). Generated output, never
# served by the app and never imported by it, so it is not committed.
storybook-static/
*.local

# Test output
coverage/

# Environment — never commit real values
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.tsbuildinfo
