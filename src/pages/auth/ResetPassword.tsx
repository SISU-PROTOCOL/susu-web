import { PagePlaceholder } from '@/components/PagePlaceholder';

export function ResetPassword() {
  return (
    <PagePlaceholder
      phase="Phase 4"
      title="Choose a new password"
      description="This screen completes the recovery flow initiated from the reset link sent to your email."
    />
  );
}
