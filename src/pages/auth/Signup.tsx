import { PagePlaceholder } from '@/components/PagePlaceholder';

export function Signup() {
  return (
    <PagePlaceholder
      phase="Phase 4"
      title="Create your account"
      description="Signing up requires email verification before the account becomes usable. Wallet linking is a separate step and is never required to create an account."
    />
  );
}
