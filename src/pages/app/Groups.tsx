import { PagePlaceholder } from '@/components/PagePlaceholder';

export function Groups() {
  return (
    <PagePlaceholder
      phase="Phase 6"
      title="Groups"
      description="Every Susu group you belong to, with its contribution amount, frequency, member count, and current status."
    />
  );
}
