import { PagePlaceholder } from '@/components/PagePlaceholder';

export function Activity() {
  return (
    <PagePlaceholder
      phase="Phase 7"
      title="Activity"
      description="Contributions, payouts, joins, and group completions across your groups, derived from indexed chain events and verified against contract state."
    />
  );
}
