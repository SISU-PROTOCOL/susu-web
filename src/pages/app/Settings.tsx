import { PagePlaceholder } from '@/components/PagePlaceholder';

export function Settings() {
  return (
    <PagePlaceholder
      phase="Phase 7"
      title="Settings"
      description="Profile details, optional profile photo, connected wallet, notification preferences, password change, and account deletion."
    />
  );
}
